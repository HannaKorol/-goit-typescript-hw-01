const age: number = 50;
const username: string = "Max";
const toggle: boolean = true;
const empty: null = null; //можна не вказувати, але краще визначити що empty = null
const callback = (a: number): number => {
  return 100 + a;
};
